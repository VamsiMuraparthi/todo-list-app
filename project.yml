name: TodoListApp
options:
  bundleIdPrefix: com.vamsi
targets:
  TodoListApp:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - TodoListApp
    settings:
      INFOPLIST_FILE: TodoListApp/Info.plist
      PRODUCT_BUNDLE_IDENTIFIER: com.vamsi.TodoListApp
      MARKETING_VERSION: "1.0.0"
      CURRENT_PROJECT_VERSION: "1"
